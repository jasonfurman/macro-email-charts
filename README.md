# macro-email-charts

Public image host for Jason Furman's macro release-day emails. The emailer (jasonfurman/chartbook `mailer/`) pushes each release's chart PNGs here and links them by raw URL, so they render inline in Gmail (iPhone/iPad/web) and Mimestream with no attachments.
